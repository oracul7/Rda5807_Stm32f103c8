#ifndef __MAIN_H
#define __MAIN_H

//----------------------------------------------------------------------------------------------
// подключаем библиотеки
//----------------------------------------------------------------------------------------------
#include "stm32f10x.h"

#include "mcu_ini.h"
#include "delay.h"

//----------------------------------------------------------------------------------------------
// объявляем дефайны
//----------------------------------------------------------------------------------------------
#define LED_ON		GPIO_SetBits(PORTC, LED);			// Устанавливаем лог 1(т.е. +5в) на выводе
#define LED_OFF		GPIO_ResetBits(PORTC, LED);		// Сбрасываем лог 1(т.е. 0в) на выводе

#endif // __MAIN_H
